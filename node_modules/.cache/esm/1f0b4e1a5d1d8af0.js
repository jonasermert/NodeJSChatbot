let MAIN_CLIENT,answer,BOT_PREFIX;_5d1‍.w("./clients",[["MAIN_CLIENT",["MAIN_CLIENT"],function(v){MAIN_CLIENT=v}]]);_5d1‍.w("./functions",[["answer",["answer"],function(v){answer=v}]]);_5d1‍.w("./constants",[["BOT_PREFIX",["BOT_PREFIX"],function(v){BOT_PREFIX=v}]]);

//Functions


//Triggers





MAIN_CLIENT.connect();
MAIN_CLIENT.on("message", (channel, userstate, message, self) => {
    if (self) return;

    switch (userstate["message-type"]){
        case "action":
            //Do Something
            break;
        case "chat":
            // Wenn nachricht mit ! beginnt
            if (message.charAt(0) === BOT_PREFIX){
                answer(MAIN_CLIENT, message)
            }

            break;
        case "whisper":
            //Do Something
            break;
        default:
            //Do Something
            break;
    }

});